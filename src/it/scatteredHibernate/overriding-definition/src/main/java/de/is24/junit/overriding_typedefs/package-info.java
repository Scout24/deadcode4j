@TypeDefs({
        @TypeDef(name = "numberClass", typeClass = Number.class)})
package de.is24.junit.overriding_typedefs;

import org.hibernate.annotations.TypeDef;
import org.hibernate.annotations.TypeDefs;